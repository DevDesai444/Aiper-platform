# Test / development dependencies (not installed into the production image).
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate from requirements.txt so the runtime image stays lean. E10 owns
# CI pinning; align these there if versions need locking.
-r requirements.txt

pytest
pytest-asyncio
httpx
