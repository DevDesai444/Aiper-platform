# Unpinned on purpose: every image build resolves the latest release of each.

# API
fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings
email-validator

# Persistence
sqlalchemy[asyncio]
asyncpg
greenlet

# Auth
bcrypt
PyJWT

# Agent runtime
deepagents
langchain
langchain-core
langgraph
langchain-openai

# Retrieval
qdrant-client

# Document parsing (page-aware)
pypdf
python-pptx
python-docx

# auth (E1)
# Supabase JWT verification needs RS256/ES256 support -> the [crypto] extra
# (pulls in `cryptography`). Supersedes the plain PyJWT pin above.
PyJWT[crypto]
