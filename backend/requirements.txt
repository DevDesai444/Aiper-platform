# Pinned versions — resolved 2024-09 with Python 3.12.
# To upgrade: create a fresh venv, install without pins, run `pip list --format=freeze`,
# update the versions here, then run the test suite before committing.

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Persistence
sqlalchemy[asyncio]==2.1.0
asyncpg==0.31.0
greenlet==3.5.6

# Auth
bcrypt==5.0.0
PyJWT==2.15.0

# Agent runtime
deepagents==0.7.19
langchain==1.4.2
langchain-core==1.6.5
langgraph==1.2.12
langchain-openai==1.6.6

# Retrieval
qdrant-client==1.19.1

# Document parsing (page-aware)
pypdf==6.19.0
python-pptx==1.0.2
python-docx==1.2.0
